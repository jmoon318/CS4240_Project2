#start_function
void main():
int-list: acc, ctr, con
float-list: 
    assign, acc, 1
    assign, ctr, 100
loop:
    sub, con, ctr, 1
    brlt, done, con, 1
    mult acc, acc, ctr
    sub, ctr, ctr, 1
    goto loop
done:
#end_function